<h1>My tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %><br>
    <%= task.completed ? 'Yes' : 'no'%>
  </li>
  <% end %>
</ul>

<%= link_to "add new task", new_task_path %>
